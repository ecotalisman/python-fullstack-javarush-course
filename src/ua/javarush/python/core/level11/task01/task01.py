# Importing Elements from a Module

# Create a module geometry.py that contains the functions circle_area(radius) and rectangle_area(length, width).
# Then import only the circle_area function and use it in another file.

### 🇺🇦 Ukrainian version:

# Імпорт елементів з модуля

# Створіть модуль geometry.py, який містить функції circle_area(radius) та rectangle_area(length, width).
# Потім імпортуйте тільки функцію circle_area і використовуйте її в іншому файлі.

# Write your code here
from geometry import circle_area

print(circle_area(5))