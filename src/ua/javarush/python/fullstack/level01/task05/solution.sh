# Checking the Docker Version
# After installing Docker on your system, run the command to check the Docker version.
#
# Hint:
# Write the command to check the Docker version.
#
# Requirements:
# • Display the installed Docker version using the `docker --version` or `docker version` command.

## 🇺🇦 Ukrainian version:

# Перевірка версії Docker
# Після встановлення Docker на вашу систему, виконайте команду для перевірки версії Docker.
#
# Підказка:
# Напишіть команду для перевірки версії Docker.
#
# Вимоги:
# • Виведіть версію встановленого Docker за допомогою команди `docker --version` або `docker version`.

# Checking the Docker Version
sudo docker --version

