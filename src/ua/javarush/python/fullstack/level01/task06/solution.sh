# Running the "hello-world" Test Container
# Run the "hello-world" test container using Docker to make sure that Docker was installed successfully
# and containers can run. The expected result is the message "Hello from Docker".
#
# Hint:
# Write the command to run the "hello-world" test container.
#
# Requirements:
# • Run the test container named "hello-world" using Docker.
# • The container must output the message "Hello from Docker" to the console, confirming that Docker is working correctly.
#
# ## 🇺🇦 Ukrainian version:
#
# Запуск тестового контейнера "hello-world"
# Запустіть тестовий контейнер "hello-world" за допомогою Docker, щоб переконатися, що встановлення Docker пройшло успішно
# і контейнери можуть запускатися. Очікуваний результат — повідомлення "Hello from Docker".
#
# Підказка:
# Напишіть команду для запуску тестового контейнера "hello-world".
#
# Вимоги:
# • Запустіть тестовий контейнер з іменем "hello-world" за допомогою Docker.
# • Контейнер повинен виводити повідомлення "Hello from Docker" у консоль, що підтверджує коректну роботу Docker.

# Running the "hello-world" test container:
docker run hello-world