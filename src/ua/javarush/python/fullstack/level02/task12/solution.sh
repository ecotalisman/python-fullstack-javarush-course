# Viewing Container Logs
# View all logs of the container named web_app.
# Use the command to display the accumulated logs of this container.
#
# Requirements:
# • The script must use a Docker command to view the logs of the container named web_app and display the accumulated
# logs.
# • The script must identify and select the container named web_app to display its logs.
# • The command must ensure that all accumulated logs of the web_app container are displayed, without filtering
# or time limits.
#
# ## 🇺🇦 Ukrainian version:
#
# Перегляд логів контейнера
# Перегляньте всі логи контейнера з іменем web_app.
# Використайте команду, щоб відобразити накопичені логи цього контейнера.
#
# Вимоги:
# • Скрипт має використовувати команду Docker для перегляду логів контейнера з іменем web_app, щоб відобразити
# накопичені логи.
# • Скрипт має ідентифікувати та вибрати контейнер з іменем web_app для відображення його логів.
# • Команда має забезпечити відображення всіх накопичених логів контейнера web_app, без фільтрації чи обмеження
# за часом.

# Getting all logs of the container named web_app
docker logs web_app