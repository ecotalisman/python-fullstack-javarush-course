# Viewing the List of Local Docker Images
# Using the docker images command, display a list of all images available on your system.
# Visually determine which tags and sizes these images have.
#
# Requirements:
# • The script must use the docker images command to display a list of all local Docker images on the system.
# • The displayed list must contain information about the tags of each Docker image.
# • The displayed list must contain information about the sizes of each Docker image.
#
# ## 🇺🇦 Ukrainian version:
#
# Перегляд списку локальних Docker Images
# Використовуючи команду docker images, виведіть список всіх образів, доступних на вашій системі.
# Візуально визначте, які теги та розміри мають ці образи.
#
# Вимоги:
# • Скрипт повинен використовувати команду docker images для виводу списку всіх локальних Docker образів на системі.
# • Виведений список повинен містити інформацію про теги кожного Docker образа.
# • Виведений список повинен містити інформацію про розміри кожного Docker образа.

# Displaying the list of all local Docker images with tags and sizes
docker images