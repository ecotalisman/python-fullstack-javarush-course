# Using the official Python image as the base image
FROM python:3.8-slim

# Setting the working directory
WORKDIR /app

# Copying the application script to the working directory
COPY hello.py .

# Defining the command to run the application
CMD ["python", "hello.py"]