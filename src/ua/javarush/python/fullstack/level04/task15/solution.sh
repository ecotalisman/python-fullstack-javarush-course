# Stopping All Services
# Write a command that stops and removes all containers started with the docker compose up command.
# All networks created for communication between the services must also be removed.
#
# Requirements:
# • The command must stop all containers started with the docker compose up command.
# • The command must remove all containers that were started with the docker compose up command.
# • The command must remove all networks created for communication between the services
#   that were started with the docker compose up command.
#
# ## 🇺🇦 Ukrainian version:
#
# Зупинка всіх сервісів
# Напишіть команду, яка зупинить та видалить усі контейнери, запущені за допомогою команди docker compose up.
# Усі мережі, створені для взаємодії між сервісами, також мають бути видалені.
#
# Вимоги:
# • Команда має зупинити всі контейнери, запущені за допомогою команди docker compose up.
# • Команда має видалити всі контейнери, які були запущені за допомогою команди docker compose up.
# • Команда має видалити всі мережі, створені для взаємодії між сервісами, які були запущені за допомогою команди docker compose up.

# Stopping all running Docker Compose containers
docker compose down