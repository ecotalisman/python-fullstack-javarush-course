/*
Convert "150" to an integer with parseInt() and print the type

You have a variable str that contains the string "150".
Convert this string to an integer using parseInt() and assign the result to a new variable num.
Print the type of num to the console.

Requirements:
• The program must include a variable str that contains the string "150".
• The program must use parseInt() to convert the string "150" to an integer.
• The program must assign the conversion result to the variable num.
• The program must print the type of num using the typeof operator.

🇺🇦 Ukrainian version:

У вас є змінна str, яка містить рядок "150". Перетворіть цей рядок на ціле число,
використовуючи метод parseInt(), і присвойте результат новій змінній num.
Виведіть тип змінної num в консоль.

Вимоги:
• Програма повинна включати змінну str, яка містить рядок "150".
• Програма повинна використовувати метод parseInt() для перетворення рядка "150" на ціле число.
• Програма повинна присвоїти результат перетворення змінній num.
• Програма повинна вивести в консоль тип змінної num з використанням оператора typeof.

Write your code here
*/

let str = "150";
//TODO:
let num = parseInt(str)
console.log(typeof num)
