/*
Global variable and function access

Declare a global variable globalMessage and assign it the string "Hello from global scope".
Create a function showMessage that prints the value of globalMessage to the console.
Call the function so the message is printed.

Requirements:
• The program must include a global variable globalMessage assigned to "Hello from global scope".
• The program must include a function showMessage.
• The showMessage function must print the value of the global variable globalMessage.
• The program must call showMessage so the variable value is printed to the console.

🇺🇦 Ukrainian version:

Оголосіть глобальну змінну globalMessage і присвойте їй рядок "Hello from global scope".
Створіть функцію showMessage, яка виводить в консоль значення globalMessage.
Викличте функцію, щоб значення змінної виводилося в консоль.

Вимоги:
• Програма повинна включати глобальну змінну globalMessage і присвоїти їй рядок "Hello from global scope".
• Програма повинна включати функцію showMessage.
• Функція showMessage повинна виводити в консоль значення глобальної змінної globalMessage.
• Програма повинна викликати функцію showMessage, щоб значення змінної виводилося в консоль.

Write your code here
*/

// Declare a global variable
//TODO:
let globalMessage = "Hello from global scope"

// Create a function
//TODO:
function showMessage() {
    console.log(globalMessage)
}

// Call the function
//TODO:
showMessage()