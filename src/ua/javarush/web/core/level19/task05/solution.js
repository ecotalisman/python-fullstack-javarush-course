/*
Create a math message using template literals

Write a function createMathMessage that takes two numbers and returns a string in the format:
"Сума X і Y дорівнює Z", where X and Y are the provided numbers and Z is their sum.
Use template literals to insert values and perform the addition.

Requirements:
• The program must include a function named createMathMessage that takes two parameters.
• createMathMessage must use template literals to build the string.
• createMathMessage must correctly calculate the sum of the two numbers.
• createMathMessage must return a string in the format "Сума X і Y дорівнює Z", where X and Y are the parameters and Z is their sum.

🇺🇦 Ukrainian version:

Напиши функцію createMathMessage, яка приймає два числа і повертає рядок у форматі
"Сума X і Y дорівнює Z", де X і Y - передані числа, а Z - їх сума.
Використовуй шаблонні літерали для вставки значень і виконання операції додавання.

Вимоги:
• Програма повинна включати функцію з ім'ям createMathMessage, яка приймає два параметри.
• Функція createMathMessage повинна використовувати шаблонні літерали для створення рядка.
• Функція createMathMessage повинна коректно розраховувати суму двох переданих їй чисел.
• Функція createMathMessage повинна повертати рядок у форматі "Сума X і Y дорівнює Z", де X і Y - передані параметри, а Z - їх сума.

Write your code here
*/

function createMathMessage(x, y) {
    //TODO:
    return `Sum ${x} and ${y} equals ${x + y}`
}
